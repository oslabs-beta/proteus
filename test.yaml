
      apiVersion: batch/v1
      kind: Job
      metadata: 
        name: testtest3
      spec: 
        template:
          spec:
            containers:
            - name: whalesay
              image: docker/whalesay
              command: ["cowsay","This is a dog"]
            restartPolicy: Never
        backoffLimit: 3
    
