
      apiVersion: batch/v1
      kind: Job
      metadata: 
        name: testdog
      spec: 
        template:
          spec:
            containers:
            - name: whalesay
              image: docker/whalesay
              command: ["cowsay"]
            restartPolicy: OnFailure
        backoffLimit: 3
    
